# angular-plugin

Content:

- **build.bat**: batch file that build the angular project and create the zip file to be uploaded to XCALLY
- **plugin**: contains the plugin files (manifest, client, server, ...)
- **project**: contains the angular project

## How to build

To build the plugin:

```bat
cd project
npm ci
cd ..
build.bat
```

