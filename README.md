# angular-plugin

Content:

- **build.bat**: batcj file that build the angular project and create the zip file to be uploaded to XCALLY
- **plugin**: contains the plugin files (manifest, client, server, ...)
- **project**: contains the angular project
